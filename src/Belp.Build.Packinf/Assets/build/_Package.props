<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <!-- Imports only on single-targeting builds -->
  <Import Project="$(MSBuildThisFileDirectory)..\buildMultiTargeting\PackageAssets.props" Condition="'$(TargetFramework)' == ''" />

  <Import Project="$(MSBuildThisFileDirectory)SetPackageReadme.props" />

</Project>
